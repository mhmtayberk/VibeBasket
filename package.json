{
  "name": "vibebasket-monorepo",
  "private": true,
  "description": "Ninite for vibe coding. Bundle your AI dev setup across multiple IDEs.",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "catalog:sync": "pnpm --filter @vibebasket/core build && pnpm --filter @vibebasket/registry build && node scripts/catalog-sync.mjs",
    "catalog:sync:dry": "pnpm --filter @vibebasket/core build && pnpm --filter @vibebasket/registry build && node scripts/catalog-sync.mjs --dry-run",
    "dev": "pnpm -r run dev",
    "test": "pnpm -r run test",
    "lint": "biome check .",
    "format": "biome format --write .",
    "typecheck": "pnpm -r run typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "typescript": "^5.7.3",
    "vitest": "^3.2.6"
  }
}
